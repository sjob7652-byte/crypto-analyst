# 🌉 تفعيل جسر أخبار X عبر Telegram (مرة واحدة — ~5 دقائق)

الجسر يقرأ قنوات Telegram **عامة** تنقل أخبار X والميم كوينز لحظة بلحظة،
ويفلترها بالكلمات المفتاحية (Solana، عقد جديد، إطلاق...) ثم يحقنها في سياق
الأخبار الذي يستعمله المحلل. مجاني 100% — لا X API مدفوع، لا سكريبنغ محظور.

> **خصوصية:** الجسر يعمل بوضع **القراءة فقط** على قنوات عامة — لا يرسل أي
> رسالة ولا ينشر أي شيء باسمك. الـsession string مفتاح حسابك: خزّنه في
> GitHub Secrets فقط ولا تشاركه مع أحد.

## الخطوة 1 — تطبيق Telegram مجاني (دقيقتان)

1. ادخل إلى https://my.telegram.org وسجّل الدخول برقم هاتفك (كود يصلك على Telegram).
2. اختر **API development tools** → **Create application**.
3. املأ أي اسم/منصة → ستحصل على `api_id` و `api_hash`. انسخهما.

## الخطوة 2 — توليد session string (على حاسوبك الخاص)

```bash
pip install telethon
python gen_session.py
```

- أدخل `API_ID` و `API_HASH`.
- أدخل الكود الذي يصلك على Telegram.
- انسخ السطر الطويل الناتج كاملاً — هذا هو `TG_SESSION`.

## الخطوة 3 — أسرار GitHub

في صفحة المستودع: **Settings → Secrets and variables → Actions → New repository secret**،
أضف أربعة أسرار:

| الاسم | القيمة |
|---|---|
| `TG_API_ID` | الرقم من الخطوة 1 |
| `TG_API_HASH` | النص من الخطوة 1 |
| `TG_SESSION` | السطر الطويل من الخطوة 2 |
| `XBRIDGE_CHANNELS` | أسماء قنوات عامة مفصولة بفواصل، مثال: `whale_alert,BitcoinMagazine` |

> اختر قنوات عامة تتابعها وتنقل أخبار الكريبتو/Mيم كوينز من X — كلما كانت
> أنشط وأسرع، كان الجسر أنفع. يمكنك تعديل القائمة في أي وقت من نفس الـsecret.

## الخطوة 4 — تحقق

من الجولة القادمة للسكانر ستظهر في سجل التشغيل سطور مثل:

```
xbridge: 7 عناصر من 2 قنوات
أخبار X⇄TG: 7 عناصر من قنوات Telegram
```

وإن غابت الأسرار أو تعطلت القنوات، يشتغل البوت عادي بدون الجسر — لا شيء ينكسر.

## ملاحظات

- عناصر الجسر تُوسم بـ `tier=3` (أدنى ثقة) — تُستعمل **كإشارات تلميحية فقط**،
  ولا تُعامل معاملة الأخبار الموثوقة في قرار الخبير.
- لا تضع `XBRIDGE_CHANNELS` فارغاً وإلا بقي الجسر معطلاً.
- لتدوير المفتاح: احذف الـsecret وأعد الخطوة 2، أو أنهِ الجلسات من
  Telegram → الإعدادات → الأجهزة.
