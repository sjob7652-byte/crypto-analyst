{
  "id": "cron-scan-1min-20260925",
  "cmd": "crontab -l 2>/dev/null | sed 's|^\\*/2 \\(\\*\\)|* \\1|' | crontab - && echo '--- CRON NOW ---' && crontab -l | grep 'bot/run_'",
  "timeout": 60,
  "note": "speed up scanner */2 -> every minute (max safe free-API cadence)"
}
