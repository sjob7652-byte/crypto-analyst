# إصدارات مثبتة (Supply Chain Security): لا تستخدم إصدارات عائمة.
# هذه الإصدارات مُختبرة وتعمل في الإنتاج بتاريخ 2026-09-21.
requests==2.34.2
telethon==1.45.0
# ملاحظة أمنية (2026-09-23): أُزيلت feedparser — غير مستعملة في أي ملف
# (الـRSS يُحلل بـxml.etree مباشرة)، ولها ثغرة ReDoS معلنة سنة 2026
# بلا إصلاح من المشرف — إزالتها تمحو سطح الهجوم كاملاً.
# --- الطبقة البحثية الثقيلة (2026-09-25): كلها اختيارية في الكود
# (fail-safe — غيابها لا يوقف الفحص). إصدارات بعجلات aarch64 لـPython 3.12.
duckdb==1.1.3          # مخزن السوق المحلي (market.duckdb)
onnxruntime==1.20.1    # استدلال نموذج المشاعر المحلي (ONNX)
scikit-learn==1.5.2    # معايرة الاحتمالات (LogisticRegression)
