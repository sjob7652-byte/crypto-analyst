# 🤖 دليل إعداد بوت محلل عملات الميم (خطوة بخطوة)

كل شيء مجاني 100%. ستحتاج حوالي 10 دقائق.

---

## الخطوة 1: أنشئ بوت Telegram (دقيقتان)

1. افتح Telegram وابحث عن **BotFather** (الحساب الرسمي بعلامة التوثيق ✅)
2. أرسل له: `/newbot`
3. اختر اسماً للبوت (مثلاً: `محلل الميم الخاص بي`)
4. اختر username ينتهي بـ `bot` (مثلاً: `ayoub_meme_bot`)
5. سيعطيك **التوكن** — نص طويل مثل: `123456:ABC-DEF...`
   - 📋 انسخه واحتفظ به (لا تشاركه مع أحد)

## الخطوة 2: اعرف رقم المحادثة Chat ID (دقيقة)

1. ابحث في Telegram عن **userinfobot** وأرسل له `/start`
2. سيرد عليك برقم الـ **Id** الخاص بك — انسخه
3. (بديل: أرسل أي رسالة لبوتك الجديد، ثم افتح في المتصفح:
   `https://api.telegram.org/bot<التوكن>/getUpdates` وابحث عن `"id"` داخل `chat`)

## الخطوة 3: أنشئ حساب GitHub مجاني (3 دقائق)

1. ادخل إلى [github.com](https://github.com) وسجّل حساباً مجانياً
2. اضغط **New repository** (زر أخضر)
3. اسم المستودع: `crypto-analyst`
4. اختر **Public** (مهم: هكذا التشغيل التلقائي مجاني بلا حدود — الكود لا يحتوي أي أسرار)
5. اضغط **Create repository**

## الخطوة 4: ارفع ملفات البوت (دقيقتان)

1. في صفحة المستودع الجديد اضغط **uploading an existing file**
2. من الملف المضغوط `crypto-analyst.zip` استخرج كل الملفات
3. اسحب **كل الملفات والمجلدات** (بما فيها مجلد `.github`) إلى صفحة الرفع
4. اضغط **Commit changes**

## الخطوة 5: أضف التوكن والـ Chat ID كأسرار (دقيقتان)

1. في صفحة المستودع: **Settings** ← **Secrets and variables** ← **Actions**
2. اضغط **New repository secret** وأضف:
   - الاسم: `TELEGRAM_BOT_TOKEN` — القيمة: التوكن من الخطوة 1
   - الاسم: `TELEGRAM_CHAT_ID` — القيمة: الرقم من الخطوة 2
3. ⚠️ الصقهما بنفسك — لا ترسلهما لأي شخص (حتى أنا لا أحتاجهما)

## الخطوة 6: شغّل البوت ✅

1. اذهب إلى تبويب **Actions** في المستودع
2. ستجد مهمة `Memecoin Scanner` تعمل تلقائياً **كل 30 دقيقة**
3. للتجربة الفورية: اضغط **Run workflow** ← **Run workflow**
4. انتظر دقيقة ثم افتح Telegram — ستصلك رسائل البوت! 🎉

---

## ماذا سيرسل لك البوت؟

- 🟢 **إشارة شراء**: عملة جديدة + النتيجة/100 + الأسباب + أهداف البيع ووقف الخسارة
- 🔴 **تحذير**: عملة خطيرة (نصب / Honeypot) لتتجنبها
- 🎯 **تنبيه بيع**: عند تحقق كل هدف سعري أو تفعيل وقف الخسارة
- 📰 **ملخص صباحي ومسائي**: الصفقات المفتوحة + أكبر التحركات + عناوين الأخبار

## تخصيص (اختياري)

كل الإعدادات في ملف `config.py`:
- `WATCHLIST`: عملات Binance التي تريد متابعتها
- `TAKE_PROFITS` / `STOP_LOSS`: أهدافك ونسبة وقف الخسارة
- `SCAN_LIMIT`: عدد العملات المفحوصة كل دورة
- `CHAINS`: الشبكات (`bsc`, `solana`, `base`, `ethereum`)

## ملاحظات مهمة

- ⏱️ أول فحص مجدول قد يتأخر قليلاً — استعمل **Run workflow** للتجربة
- 📊 حالة الصفقات تُحفظ تلقائياً في `state.json`
- ⚠️ البوت **لا يتداول بأموالك** — فقط ينبّهك، والقرار والتنفيذ لك
- 🧪 جرّب أولاً، وراقب دقة الإشارات بمبالغ صغيرة جداً قبل أي مبلغ حقيقي
